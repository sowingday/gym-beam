/**
 * lib/localTemplates.js
 *
 * Lokale Fallback-Templates für die Workout-Datenbank.
 * Wird nur verwendet, wenn die Online-DB nicht verfügbar ist.
 * Keine eingebauten Demo-Templates — leeres Array als Fallback.
 */

export const LOCAL_TEMPLATES = [];